name: Keep Supabase Alive

on:
  schedule:
    # Ejecutar cada 3 días a las 12:00 UTC (9:00 AM hora CDMX)
    - cron: '0 12 */3 * *'
  workflow_dispatch: # Permite ejecutar manualmente desde GitHub UI

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Query Supabase Database
        run: |
          response=$(curl -s -X GET '${{ secrets.SUPABASE_URL }}/rest/v1/keep_alive?select=*&limit=1' \
            -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}")
          echo "Response: $response"
          if [ -z "$response" ] || [ "$response" = "[]" ]; then
            echo "❌ Error: No data returned from keep_alive table"
            exit 1
          fi

      - name: Update Keep-Alive Timestamp
        run: |
          curl -X POST '${{ secrets.SUPABASE_URL }}/rest/v1/rpc/update_keep_alive' \
            -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'
        continue-on-error: true

      - name: Verify Success
        run: echo "✅ Supabase database activity registered successfully!"
